@model IEnumerable<AccountsReceivable.Models.Customer>

@{
    ViewBag.Title = "Customers";
}

<!-- Header -->
<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <a class="btn btn-success" href="@Url.Action("Create", "Customers")">
            <i class="fa fa-plus"></i> CREATE NEW
        </a>
    </div>
    <div class="text-center flex-grow-1">
        <div class="text-muted text-uppercase small fw-bold" style="letter-spacing: 2px;">Customers</div>
        <h1 class="h3 fw-bold"><span style="color: #0b5566;">Manage</span> customers</h1>
    </div>
    <div style="min-width: 120px;"></div>
</div>

<div class="card border-0 shadow-sm">
    <div class="table-responsive">
        <table class="table table-hover mb-0">
            <thead class="table-light">
                <tr>
                    <th>Code</th>
                    <th>Name</th>
                    <th>Tax Id</th>
                    <th>Email</th>
                    <th>Phone</th>
                    <th>Credit Limit</th>
                    <th class="text-center">Actions</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.CustomerCode</td>
                            <td>@item.Name</td>
                            <td>@item.TaxId</td>
                            <td>@item.Email</td>
                            <td>@item.Phone</td>
                            <td>@String.Format("{0:C}", item.CreditLimit)</td>
                            <td class="text-center">
                                <a class="btn btn-sm btn-info" href="@Url.Action("Details", "Customers", new { id = item.CustomerId })" title="Details">
                                    <i class="fa fa-eye"></i>
                                </a>
                                <a class="btn btn-sm btn-warning" href="@Url.Action("Edit", "Customers", new { id = item.CustomerId })" title="Edit">
                                    <i class="fa fa-pen"></i>
                                </a>
                                @using (Html.BeginForm("Delete", "Customers", FormMethod.Post,
                                    new { @class = "d-inline", onsubmit = "return confirm('Delete this customer?');" }))
                                {
                                    @Html.AntiForgeryToken()
                                    @Html.Hidden("id", item.CustomerId)
                                    <button type="submit" class="btn btn-sm btn-danger" title="Delete">
                                        <i class="fa fa-trash"></i>
                                    </button>
                                }
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="7" class="text-center py-5 text-muted">
                            <i class="fa fa-users" style="font-size: 36px; opacity: .35; display: block; margin-bottom: 12px;"></i>
                            No customers found.
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>